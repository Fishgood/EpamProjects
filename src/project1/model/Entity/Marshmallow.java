package project1.model.Entity;

/**
 * The type Marshmallow.
 */
public class Marshmallow extends Candy{

    /**
     * Instantiates a new Marshmallow.
     *
     * @param price       the price
     * @param sugarWeight the sugar weight
     * @param totalWeight the total weight
     */
    public Marshmallow(int price, int sugarWeight, int totalWeight) {
        super(price, sugarWeight, totalWeight);
    }
}
